# OS files
.DS_Store
Thumbs.db

# Archives and extracted bundles (do not commit)
*.zip
apps/sdkwork-notes-pc/

# Editors
.idea/
.vscode/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# JavaScript workspace artifacts
**/node_modules/
**/.turbo/
**/dist/
**/coverage/
**/*.tsbuildinfo

# Rust / Tauri build artifacts
/target/
**/src-tauri/target/

# Local environment files
**/.env.local
**/.env.*.local

# SDKWork local environment and runtime config overrides
.env.local
.env.development.local
.env.test.local
.env.staging.local
.env.production.local
.env.postgres
.env.release.local
config/*.local.toml

# SDKWork local runtime state
.runtime/
**/.runtime/
**/target/
!.env.example
!.env.*.example
!.env.postgres.example
**/.pnpm-store/
**/.sdkwork/local/
**/.sdkwork/secrets/
**/.sdkwork/cache/
**/.sdkwork/tmp/
**/.sdkwork/dart/pub-cache/
**/.sdkwork/manual-backups/
**/.sdkwork/sdkwork-generator-changes.json
**/.sdkwork/sdkwork-generator-manifest.json
**/.sdkwork/sdkwork-generator-report.json

# SDKWork local-only and generated artifacts
.env
.env.*
!.env.example
!.env.*.example
!.env.standalone.*
**/build/
*.log
.DS_Store
Thumbs.db
# Runtime-generated environment configuration
**/public/runtime-env.json

# Build artifacts

*.js.map
*.d.ts.map
# `tsconfig.json` includes `scripts` and the pc app root, so those are source
# directories here and tsc emit lands beside them, not only under src/.
sdkwork-notes-pc-react/scripts/**/*.js
sdkwork-notes-pc-react/scripts/**/*.d.ts

# Compiler emit is ignored only where the file is emit BY DEFINITION. The broad
# `**/src/**/*.js` / `**/src/**/*.d.ts` globs are gone: `.gitignore` cannot express "ignore
# this only when a same-stem .ts sibling exists", so they also hid authored declarations
# (`vite-env.d.ts`, `styles.d.ts`, module shims) and real mini-program source under `src/`.
# The sibling rule is enforced against tracked files by
# `sdkwork-specs/tools/check-repository-baseline-content.mjs` instead.
